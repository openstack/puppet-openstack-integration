- hosts: all
  tasks:
    - name: Ensure legacy workspace directory
      file:
        path: "{{ ansible_user_dir }}/workspace"
        state: directory

    - block:
      - name: Clean-up system state (CentOS/RHEL)
        dnf:
          name: "{{ item }}"
          state: absent
        become: true
        ignore_errors: true
        with_items:
          - epel-release
          - rdo-release
          - centos-release-openstack-*
          - centos-release-ceph-*

      - name: Reset dnf modules (CentOS/RHEL)
        shell:
          cmd: |-
            dnf module -y reset {{ item.key }}
        become: true
        with_items: "{{ dnf_modules | default({}) | dict2items }}"

      - name: Enable dnf modules (CentOS/RHEL)
        shell:
          cmd: |-
            dnf module -y enable {{ item.key }}:{{ item.value }}
        become: true
        with_items: "{{ dnf_modules | default({}) | dict2items }}"

      - name: Install Ruby dependencies (CentOS/RHEL)
        dnf:
          name: "{{ item }}"
        become: true
        with_items:
          - "@Development tools"
          - libxml2-devel
          - libxslt-devel
          - ruby-devel
          - zlib-devel
          - rubygem-rexml

      when:
        - ansible_os_family == 'RedHat'

    - name: Install Ruby dependencies (Ubuntu)
      apt:
        name: "{{ item }}"
      become: true
      when:
        - ansible_os_family == 'Debian'
        - ansible_distribution == "Ubuntu"
      with_items:
        - libxml2-dev
        - libxslt-dev
        - ruby-dev
        - zlib1g-dev

    - name: Install Ruby dependencies (Debian)
      apt:
        name: "{{ item }}"
      become: true
      when:
        - ansible_os_family == 'Debian'
        - ansible_distribution == "Debian"
      with_items:
        - libicu-dev
        - libxml2-dev
        - libxslt1-dev
        - ruby-dev
        - zlib1g-dev

    # TODO(tkajinam): Remove this once libvirt 10.0.0 is available
    - block:
      - name: Workaround for https://issues.redhat.com/browse/RHEL-20609
        shell:
          cmd: |
            dnf config-manager --save --setopt exclude=libvirt*-9.10.0-1.el9
        become: true
      when:
        - ansible_os_family == 'RedHat'

    # TODO(tkajinam): Remove this one the issue is resolved in cs9
    - block:
      - name: Workaround for https://issues.redhat.com/browse/RHEL-25802
        shell:
          cmd: |
            dnf module disable -y mariadb:10.11
        become: true
      when:
        - ansible_os_family == 'RedHat'
